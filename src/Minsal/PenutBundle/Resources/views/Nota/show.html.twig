{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nota</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Ciclo</th>
                <td>{{ entity.ciclo }}</td>
            </tr>
            <tr>
                <th>Notafinal</th>
                <td>{{ entity.notaFinal }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('nota') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('nota_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
